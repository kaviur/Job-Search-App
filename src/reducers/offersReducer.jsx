import { types } from '../types/types'

const offersReducers = ( state = {}, action) => {

    switch( action.type) {

        default:
            return state;

    }
}

export default offersReducers;